{
  "generatedBy": "tools/gen_disk_layouts.nim",
  "source": "repro/disk_layouts.nim",
  "defaultLayout": "uefi-ext4",
  "defaultDiskSizeGb": 8,
  "defaultEspSizeMib": 512,
  "minEspSizeMib": 128,
  "presets": [
    {
      "name": "uefi-ext4",
      "summary": "ESP + a single writable ext4 root; the layout every ReproOS image has shipped with",
      "buildable": true,
      "unbuildableReason": "",
      "minDiskSizeGb": 4,
      "defaultEspSizeMib": 512
    },
    {
      "name": "uefi-attested",
      "summary": "ESP + a read-only root + separate /var, /home and swap; the shape an attestable image needs",
      "buildable": false,
      "unbuildableReason": "the integrity-checked root image is built, but the image driver does not install it onto this layout yet and boot still goes through GRUB, whose command line is not measured; the result would be a read-only root whose root hash nothing attests to",
      "minDiskSizeGb": 16,
      "defaultEspSizeMib": 512
    }
  ]
}
